// import { createClient } from '@liveblocks/client';
// import { createRoomContext } from '@liveblocks/react';

// const client = createClient({ authEndpoint: '/api/liveblocks-auth' });

// export type Message = {
//   id: string;
//   userId: string;
//   name?: string;
//   image?: string;
//   content: string;
//   createdAt: number;
// };

// export type RoomSchema = {
//   messages: Message[];
// };

// export type Presence = {};

// export const { RoomProvider, useStorage, useMutation } = createRoomContext<
//   RoomSchema,
//   Presence
// >(client);
