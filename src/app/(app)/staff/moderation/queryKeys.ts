/**
 * Query key for moderation inbox queries.
 */
export const moderationInboxQueryKey = ['moderation', 'inbox'] as const;

/**
 * Query key for removed items queries.
 */
export const removedItemsQueryKey = ['moderation', 'removed'] as const;
